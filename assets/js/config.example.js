/* =====================================================================
   Speakly — frontend config (Supabase)
   ---------------------------------------------------------------------
   HOW TO USE:
   1. Copy this file to "config.js" in the same folder.
   2. Paste your Supabase Project URL and the ANON / PUBLIC key below.
   3. Add <script src="assets/js/config.js"></script> in index.html / app.html
      BEFORE the Supabase library and app.js.

   ⚠️ SECURITY
   - Only the "anon" / "public" key goes here. It is safe for the browser.
   - NEVER put the "service_role" key in frontend code or in git.
   ===================================================================== */

window.SPEAKLY_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT-REF.supabase.co",
  SUPABASE_ANON_KEY: "PASTE-YOUR-ANON-PUBLIC-KEY-HERE",
};
