/* =====================================================================
   Speakly — frontend config (Supabase)
   ---------------------------------------------------------------------
   1. Replace the two values below with your own Supabase project values:
        Dashboard → Project Settings → API
        - Project URL      → SUPABASE_URL
        - anon / public key → SUPABASE_ANON_KEY
   2. Only the "anon" / "public" key goes here. It is SAFE for the browser.
      NEVER put the "service_role" key in frontend code.
   ===================================================================== */

window.SPEAKLY_CONFIG = {
  SUPABASE_URL: "https://ljxmqkemudexwksmygyj.supabase.co",
  SUPABASE_ANON_KEY: "sb_publishable_QJNiMStGkjKh4l6S7J0_SA_KYAMJ-Ud",
};
